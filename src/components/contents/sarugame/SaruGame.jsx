import { createContext } from "react"

// eslint-disable-next-line react-refresh/only-export-components
export const changeSceneContext = createContext()
// eslint-disable-next-line react-refresh/only-export-components
export const sceneContext = createContext()

function SaruGame() {
  
  return (
    <div>SaruGame</div>
  )
}

export default SaruGame